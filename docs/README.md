# xdv-os Docs

This directory documents the `xdv-os` integration workspace.

## Files

- `build_pipeline.md`: build stages, toolchain requirements, and outputs.
- `boot_contract.md`: stage0/boot.bin/kernel ownership and handoff boundaries.
- `image_layout.md`: 64 MB image partition/layout profile and preload policy.
- `ci_virtualbox_profile.md`: enforced VirtualBox CI boot profile and pass/fail criteria.

For broader architecture context, also see:

- `../xdv_white_paper.md`
- `../README.md`
